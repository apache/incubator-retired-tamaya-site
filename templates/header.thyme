<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-transitional-thymeleaf-4.dtd">
<html lang="en">
	<head th:fragment="head">
	    <meta charset="utf-8"/>
	    <title th:with="title=(${content.title != null} ? ${content.title} : 'Tamaya Incubator')" th:text="${title}"></title>
	    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
	    <meta name="description" content="Homepage of Apache Tamaya (incubating)"/>
	    <meta name="author" content="Apache Tamaya Project Team"/>
	    <meta name="keywords" content="Apache Tamaya Incubating, configuration, Java, ASF, Apache Software Foundation"/>
      <meta name="generator" th:with="jbakeversion='JBake ${content.version}'" th:content="${jbakeversion}"/>

	    <!-- Le styles -->
	    <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'css/bootstrap.min.css'" rel="stylesheet"/>
	    <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'css/asciidoctor.css'" rel="stylesheet"/>
	    <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'css/base.css'" rel="stylesheet"/>
	    <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'css/prettify.css'" rel="stylesheet"/>

	    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
	    <!--[if lt IE 9]>
	      <script th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:src="${rootpath}+'js/html5shiv.min.js'"></script>
	    <![endif]-->

	    <!-- Fav and touch icons from ASF -->
			<link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="shortcut icon" th:href="${rootpath}+'favicon.ico'"/>
			<link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="57x57" th:href="${rootpath}+'favicons/apple-touch-icon-57x57.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="60x60" th:href="${rootpath}+'favicons/apple-touch-icon-60x60.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="72x72" th:href="${rootpath}+'favicons/apple-touch-icon-72x72.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="76x76" th:href="${rootpath}+'favicons/apple-touch-icon-76x76.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="114x114" th:href="${rootpath}+'favicons/apple-touch-icon-114x114.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="120x120" th:href="${rootpath}+'favicons/apple-touch-icon-120x120.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="144x144" th:href="${rootpath}+'favicons/apple-touch-icon-144x144.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="152x152" th:href="${rootpath}+'favicons/apple-touch-icon-152x152.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="apple-touch-icon" sizes="180x180" th:href="${rootpath}+'favicons/apple-touch-icon-180x180.png'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="icon" type="image/png" th:href="${rootpath}+'favicons/favicon-32x32.png'" sizes="32x32"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="icon" type="image/png" th:href="${rootpath}+'favicons/favicon-194x194.png'" sizes="194x194"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="icon" type="image/png" th:href="${rootpath}+'favicons/favicon-96x96.png'" sizes="96x96"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="icon" type="image/png" th:href="${rootpath}+'favicons/android-chrome-192x192.png'" sizes="192x192"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="icon" type="image/png" th:href="${rootpath}+'favicons/favicon-16x16.png'" sizes="16x16"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="manifest" th:href="${rootpath}+'favicons/manifest.json'"/>
		  <link th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" rel="shortcut icon" th:href="${rootpath}+'favicons/favicon.ico'"/>
		  <meta name="msapplication-TileColor" content="#603cba"/>
		  <meta th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" name="msapplication-TileImage" th:content="${rootpath}+'favicons/mstile-144x144.png'"/>
		  <meta th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" name="msapplication-config" th:content="${rootpath}+'favicons/browserconfig.xml'"/>
		  <meta name="theme-color" content="#303284"/>
	</head>
</html>
